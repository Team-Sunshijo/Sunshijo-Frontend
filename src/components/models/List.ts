export interface ListProps {
      date: string;
      grade_class: string;
      subject: string;
      teacher: string;
}

export interface TimetableListProps{
      date: string;
      grade_class: string;
      subject: string;
      teacher: string;
      reason:string;
      state:string;
}

